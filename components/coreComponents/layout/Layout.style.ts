import styled from "@emotion/styled";

export const SLayout = styled.div`
 height: 100%;
.for-footer-stay-bottom{
    min-height: 100%;
    margin-bottom: -400px;
    &::after {
      content: "";
      display: block;
      height: 482px;
    }
}

`