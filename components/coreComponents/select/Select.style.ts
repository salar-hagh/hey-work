import styled from "@emotion/styled";
import { theme } from "../../../theme/theme";

export const SSelect = styled.div`
  margin-bottom: 15px;
  label {
    display: block;
    margin-bottom: 5px;
    text-align: right;
  }

  select{
 
  }
`;
